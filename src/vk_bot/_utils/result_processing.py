"""Result processing — DELETED.

This module was removed as part of the handler refactoring.
Handlers now use ``VKHandlerContext`` methods (``.reply()``, ``.edit()``, etc.)
to send messages directly, instead of returning ``VKHandlerResult``.
"""
